<% form_for @employee_tax, :url=>employee_taxes_path do |f| %>
  <%= render :partial=>'form', :locals=>{:f=>f} %>
  <p class="submit">
    <%= f.submit 'Create',:class=>'btn' %> <%= "#{t('common.or')}" %>
    <%= link_to('Cancel',employee_taxes_path, :class=>'cancel') %>
  </p>
<% end %>